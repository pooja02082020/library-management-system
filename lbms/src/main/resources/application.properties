server.port=8083
 
 
spring.datasource.url=jdbc:mysql://localhost:3306/library_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=lohani
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
 
# Hibernate + JPA configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
 
# JWT settings
app.jwt.secret=ReplaceThisWithAStrongSecretKey
app.jwt.expirationMs=3600000
 
# Spring cache (simple) - no extra config needed for SimpleCacheManager
# Or Caffeine example
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=500,expireAfterAccess=10m
 
# Pagination default
app.pagination.defaultPage=0
app.pagination.defaultSize=20